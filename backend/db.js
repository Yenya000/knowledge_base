const { Pool } = require('pg');

const pool = new Pool({
    user: 'postgres',
    host: 'localhost',
    database: 'oblivion_db',
    password: '5262W6W3',
    port: 5432,
});

const initializeDatabase = async () => {
    const client = await pool.connect();
    try {
        // 1. Создаём таблицы
        await client.query(`
            CREATE TABLE IF NOT EXISTS categories (
                id SERIAL PRIMARY KEY,
                name VARCHAR(100) UNIQUE NOT NULL
            );

            CREATE TABLE IF NOT EXISTS articles (
                id SERIAL PRIMARY KEY,
                title VARCHAR(500) NOT NULL,
                content TEXT,
                category_id INT REFERENCES categories(id) ON DELETE SET NULL,
                author_id INT,
                created_at TIMESTAMPTZ DEFAULT now(),
                updated_at TIMESTAMPTZ DEFAULT now()
            );
        `);
        console.log('Таблицы categories и articles готовы');

        // 2. Удаляем старые категории и добавляем 4 нужные
        await client.query(`TRUNCATE categories CASCADE`);
        await client.query(`
            INSERT INTO categories (id, name) VALUES
            (1, 'HR'),
            (2, 'IT'),
            (3, 'Финансы'),
            (4, 'Маркетинг');
        `);
        console.log('Категории обновлены');

        // 3. Очищаем статьи и вставляем тестовые
        await client.query(`TRUNCATE articles RESTART IDENTITY`);
        const articles = [
            ['Как подключиться к корпоративной сети Wi-Fi', '1. Включите Wi-Fi на устройстве.\n2. Выберите сеть Company_Staff.\n3. Введите пароль, указанный в приветственном письме.\n4. При возникновении проблем обратитесь в IT-отдел.', 2, 1],
            ['Политика отпусков и отгулов', 'Каждый сотрудник имеет право на 28 календарных дней оплачиваемого отпуска в год. Отпуск можно разделить на части, одна из которых не менее 14 дней. Заявление подаётся за 2 недели.', 1, 1],
            ['Регламент работы с конфиденциальной информацией', 'Документы с грифом «Конфиденциально» запрещено выносить за пределы офиса. Пересылка осуществляется только через защищённые каналы. При нарушении – дисциплинарное взыскание.', 2, 2],
            ['Инструкция по заполнению табеля рабочего времени', '1. Откройте корпоративный портал. 2. Перейдите в раздел «Учёт времени». 3. Выберите период. 4. Внесите часы и сохраните. Табель отправляется в бухгалтерию до 25 числа каждого месяца.', 1, 1],
            ['Руководство по брендированию презентаций', 'Все презентации должны использовать корпоративный шаблон с логотипом в правом верхнем углу. Основные цвета: синий #005BAA и серый #6D6E71. Загрузить шаблон можно по ссылке в общем доступе.', 4, 3],
            ['Как оформить командировку', 'Заявление на командировку подаётся через HR-портал. Приложите служебную записку и приблизительную смету расходов. Срок оформления – 3 рабочих дня. Билеты приобретаются централизованно через отдел закупок.', 1, 1],
            ['Часто задаваемые вопросы по зарплатному проекту', 'Q: Когда приходит зарплата? A: Два раза в месяц – 15-го (аванс) и последнего числа. Q: Как получить справку 2-НДФЛ? A: Запросить в бухгалтерии или скачать в личном кабинете. Q: Куда обращаться по ошибкам в начислениях? A: Написать на payroll@company.ru.', 3, 1],
            ['Настройка корпоративной почты на мобильном устройстве', '1. Установите приложение Outlook. 2. Добавьте учётную запись с email сотрудника. 3. Введите пароль. 4. Сервер: outlook.office365.com. 5. Используйте SSL. Подробная инструкция с картинками во вложении.', 2, 4],
            ['Правила оформления заявок в IT-поддержку', 'При создании заявки обязательно укажите: 1. Номер кабинета. 2. Описание проблемы. 3. Скриншот ошибки. 4. Срочность (низкая/средняя/высокая). Заявки без скриншота обрабатываются дольше.', 2, 4],
            ['Кодекс корпоративной этики', 'Уважайте коллег, соблюдайте дресс-код, не используйте ненормативную лексику. Обо всех нарушениях можно сообщить через форму «Сообщить о нарушении» в личном кабинете.', 4, 2]
        ];

        const insertSQL = 'INSERT INTO articles (title, content, category_id, author_id) VALUES ($1, $2, $3, $4)';
        for (const a of articles) {
            await client.query(insertSQL, a);
        }

        console.log('Тестовые статьи добавлены');
    } catch (err) {
        console.error('Ошибка инициализации базы данных:', err);
    } finally {
        client.release();
    }
};

// Запуск
initializeDatabase();

pool.connect((err) => {
    if (err) {
        console.error('Ошибка подключения к PostgreSQL:', err.stack);
    } else {
        console.log('Ура! База данных Postgres успешно подключена!');
    }
});

module.exports = pool;